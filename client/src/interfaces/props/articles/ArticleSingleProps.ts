export interface ArticleSingleProps {
  id: string;
  className: string;
  title: string;
  image: string;
  shortContent: string;
  createdAt: string;
  categoryName: string;
  currentPage?: string;
  selectedCategory?: string;
}
